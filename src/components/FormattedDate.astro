---
export interface Props {
  dateStr: string;
  fullDate?: boolean;
}

const { dateStr, fullDate } = Astro.props;
const d = new Date(dateStr);
---

<time datetime={dateStr}>
  {fullDate ? d.toLocaleDateString() : d.toISOString().substring(0, 10)}
</time>
